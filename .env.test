# ============================================================
# 이 파일은 Git에 공유됩니다.
# 실제 AWS 자격증명 등 민감한 정보는 절대 추가하지 마세요.
# 실제 값은 .env.test.local 파일에 작성하면 덮어씁니다.
# ============================================================

NODE_ENV=test
DATABASE_URL="postgresql://test:test@localhost:5433/codiit_test"
ACCESS_TOKEN_SECRET=test-access-token-secret
ACCESS_TOKEN_EXPIRES_IN=15m
REFRESH_TOKEN_SECRET=test-refresh-token-secret
REFRESH_TOKEN_EXPIRES_IN=7d

# AWS (더미 값 - 실제 값은 .env.test.local에 작성)
AWS_REGION=dummy-region
AWS_S3_BUCKET=dummy-bucket
AWS_ACCESS_KEY_ID=dummy-access-key
AWS_SECRET_ACCESS_KEY=dummy-secret-key

# Portone 더미
PORTONE_API_KEY=dummy-access-key
PORTONE_API_SECRET=dummy-secret-key

# Redis (테스트용 로컬 컨테이너)
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_PASSWORD=
